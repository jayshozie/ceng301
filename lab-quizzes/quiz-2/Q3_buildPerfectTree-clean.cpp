#include "BinaryTree.h"
#include <cmath>

/* I failed at solving this during the quiz. */

// Q3
void BinaryTree::buildPerfectTree(int* values, int size) {
    int i = 0;
    int depth = 2^(size+1) - 1;
}
